[build]
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

[functions."upload"]
  # Increase request body size limit to 50MB for photo/video uploads
  request_body_limit = 52428800
  # Increase timeout to 60 seconds for large video uploads
  timeout = 60
